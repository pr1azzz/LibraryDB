<Window x:Class="LibraryManagementSystem.Views.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление книги" Height="400" Width="400"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Поля ввода -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="TitleBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Автор:" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="AuthorCombo" Grid.Row="1" Grid.Column="1" Margin="5" 
                  DisplayMemberPath="LastName"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Жанр:" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="GenreCombo" Grid.Row="2" Grid.Column="1" Margin="5" 
                  DisplayMemberPath="Name"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Год:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="YearBox" Grid.Row="3" Grid.Column="1" Margin="5" Text="2024"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="ISBN:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="IsbnBox" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Количество:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="QuantityBox" Grid.Row="5" Grid.Column="1" Margin="5" Text="1"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="OK" Click="OkButton_Click" Width="80" Margin="5"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>